name: KofferBuild
on:
  push:
    branches: 
      - master
  pull_request:
    branches: 
      - master
jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - name: Variable | Export Build Date
      run: |
        export varrundate="$(date +%y%m%d%I%M%S)"
        echo "::set-env name=varrundate::$varrundate"
    - name: Variable | Git Commit Hash
      run: |
        export githash="$(git log -1 --pretty=%h)"
        echo "::set-env name=githash::$githash"
    - name: Variable | Git Commit Hash
      run: |
        export gitremote="$(git config --get remote.origin.url)"
        echo "::set-env name=gitremote::$gitremote"
    - name: Variable | Webscrape OpenShift Release File for latest OC version 
      run: |
        export varveropenshift="$(curl --silent https://mirror.openshift.com/pub/openshift-v4/clients/ocp/latest/release.txt | awk '/  Version/{print $2}')"
        echo "::set-env name=varveropenshift::$varveropenshift"
    - name: Variable | Webscrape for Latest Terraform Version
      run: |
        export varverterraform="$(curl -sL https://api.github.com/repos/hashicorp/terraform/releases/latest | jq -r '.tag_name' | sed 's/v//g')"
        echo "::set-env name=varverterraform::$varverterraform"
    - name: Variable | Webscrape for Latest Helm Version
      run: |
        export varverhelm="$(curl -sL https://api.github.com/repos/helm/helm/releases/latest | jq -r '.tag_name' | sed 's/v//g')"
        echo "::set-env name=varverhelm::$varverhelm"
    - name: Variable | Webscrape Latest JQ Version
      run: |
        export varverjq="$(curl -sL https://api.github.com/repos/stedolan/jq/releases/latest | jq -r '.tag_name' | sed 's/jq-//g')"
        echo "::set-env name=varverjq::$varverjq"
#   - name: podman.io + Quay.io Login
#     run: |
#       echo "${{ secrets.podmanHUB_PASSWD }}" | podman login -u ${{ secrets.podmanHUB_USER }} --password-stdin quay.io; \
#       echo "${{ secrets.podmanHUB_PASSWD }}" | podman login -u ${{ secrets.podmanHUB_USER }} --password-stdin podman.io 
    - name: Write gitconfig
      run: |
        cat <<EOF > gitconfig
        [user]
                name = ${{ secrets.ROBOT_USER }}
                email = ${{ secrets.ROBOT_USER }}@braincraft.io
        [url "https://${{ secrets.ROBOT_USER }}:${{ secrets.ROBOT_TOKEN }}@github.com"]
                insteadOf = https://github.com
        EOF
    - name: Build Koffer Entrypoint
      run: |
        set -x; \
        cat gitconfig; \
        git clone https://${{ secrets.ROBOT_USER }}:${{ secrets.ROBOT_TOKEN }}@github.com/${{ secrets.DOCKERHUB_ORGANIZATION }}/koffer-go.git; \
        podman run --rm \
            --volume $(pwd)/gitconfig:/root/.gitconfig:z \
            --volume $(pwd)/koffer-go:/root/dev:z \
            --volume $(pwd)/koffer-go/bin:/tmp/bin:z \
            --entrypoint /root/dev/build.sh \
          docker.io/${{ secrets.podmanHUB_ORGANIZATION }}/red-gotools
    - name: Build Image | Koffer
      run: |
        podman build . \
                 --force-rm \
                 --file ./container/podmanfile \
                 --build-arg varVerJq=${{ env.varverjq }} \
                 --build-arg varVerHelm=${{ env.varverhelm }} \
                 --build-arg varRunDate=${{ env.varrundate }} \
                 --build-arg varVerTerraform=${{ env.varverterraform }} \
                 --build-arg varVerOpenshift=${{ env.varveropenshift }} \
                 --build-arg varUrlGit=${{ env.gitremote }} \
                 --tag quay.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer \
                 --tag quay.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:latest \
                 --tag quay.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:${{ env.varveropenshift }} \
                 --tag quay.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:${{ env.varveropenshift }}-${{ env.varrundate }}-${{ env.githash }} \
                 --tag podman.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer \
                 --tag podman.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:latest \
                 --tag podman.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:${{ env.varveropenshift }} \
                 --tag podman.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:${{ env.varveropenshift }}-${{ env.varrundate }}-${{ env.githash }}
    - name: Image Push | Koffer | podman.io
      run: |
        podman push podman.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer;                    \
        podman push podman.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:latest;                                 \
        podman push podman.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:${{ env.varveropenshift }};             \
        podman push podman.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:${{ env.varveropenshift }}-${{ env.varrundate }}-${{ env.githash }};
    - name: Image Push | Koffer | quay.io
      run: |
        podman push quay.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer;                    \
        podman push quay.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:latest;                                 \
        podman push quay.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:${{ env.varveropenshift }};             \
        podman push quay.io/${{ secrets.podmanHUB_ORGANIZATION  }}/koffer:${{ env.varveropenshift }}-${{ env.varrundate }}-${{ env.githash }};

#       sed -i 's/ROBOT_USER/${{ secrets.ROBOT_USER }}/g' gitconfig
#       sed -i 's/ROBOT_TOKEN/${{ secrets.ROBOT_TOKEN }}/g' gitconfig
