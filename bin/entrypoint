#!/bin/bash

# Update repo with latest from master
git_pull () {
    cd /root/deploy;
    git pull;
}

# Start Registry Server
run_registry () {
  registry serve /etc/docker/registry/config.yml &
}

run_core () {
git_pull
run_registry
}

clear
run_core
./site.yml
#/usr/bin/tmux -s Coffer
